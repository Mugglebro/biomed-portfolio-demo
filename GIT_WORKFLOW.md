# Git 版本管理工作流

## 1. 为什么这个项目要用 Git

Git 可以把每次重要修改保存成一个版本。以后如果页面改坏、脚本跑不通、邮件模板出问题，可以回到之前的稳定版本。

对这个项目来说，Git 主要用于四件事：

- 创建项目：建立一个干净的代码仓库。
- 制作项目：每次修改代码、页面或文档。
- 保存项目：把当前稳定状态保存为一个版本。
- 审核项目：在保存前检查是否误提交密码、真实邮箱、日志、Excel 数据。
- 优化项目：基于历史版本持续迭代，而不是越改越乱。

## 2. 本项目的 Git 原则

- 只管理 `zettalab_biomed_radar` 当前项目目录。
- 不提交 `.env`，因为里面可能有邮箱授权码。
- 不提交 `.xlsx`、`outputs/`、`logs/`、`backups/`，因为里面可能有真实数据或运行记录。
- 每次保存前先审核，再提交。
- 提交说明要写清楚“这次改了什么”。

## 3. 推荐操作顺序

### 第一次

1. 双击 `一键初始化Git项目.bat`。
2. 双击 `一键审核项目.bat`。
3. 双击 `一键保存版本.bat`。

### 以后每次修改后

1. 双击 `一键查看改动.bat`。
2. 双击 `一键审核项目.bat`。
3. 确认没有敏感信息后，双击 `一键保存版本.bat`。

## 4. 常用命令解释

### git status

查看哪些文件发生了变化。

### git add

把准备保存的文件放入暂存区。

### git commit

把暂存区的内容保存为一个版本。

### git log

查看历史版本。

## 5. 如何判断一次提交是否合格

一次合格提交应该满足：

- 只包含本次相关修改。
- 不包含授权码、真实邮箱密码、日志、Excel 数据。
- 页面或脚本能正常运行。
- 提交说明能看懂。

## 6. 后续可以升级的 Git 流程

- 增加分支：把新功能放在单独分支上开发。
- 增加标签：给稳定版本打 `v1.0`、`v1.1`。
- 接入 GitHub：把脱敏后的代码发布为作品项目。
- 接入 GitHub Pages：发布可访问的网页 Demo。

